import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.mainwaring.pwa',
  appName: 'Mainwaring.dev',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
